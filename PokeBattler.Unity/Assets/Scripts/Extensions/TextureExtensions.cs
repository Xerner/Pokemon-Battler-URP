using UnityEngine;

namespace PokeBattler.Client.Extensions
{
    public static class TextureExtensions
    {
        public static Vector2Int GetTrueSizeInPixels(this Texture2D texture, float ignoredAlpha)
        {
            return GetTrueSizeInPixels(texture, ignoredAlpha);
        }
    }
}